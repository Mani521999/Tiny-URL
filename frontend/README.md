# TinyLink Frontend (Dark Mode)

React + Tailwind CSS dark-mode frontend for the TinyLink URL shortener.

## Setup

```bash
cd frontend
cp .env.example .env
npm install
npm start
```

Make sure your backend is running and `REACT_APP_API_BASE_URL` points to it.
